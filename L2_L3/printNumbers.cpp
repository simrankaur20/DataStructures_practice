#include <iostream>
using namespace std;

int main() {
    int n;

    cin >> n;

    int currentNumber = 1;
    while(currentNumber <= N) {
        cout << currentNumber << endl;
        currentNumber = currentNumber + 1;
    }
    cout << "DOne";
}
